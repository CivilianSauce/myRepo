#Install sudo dengan masuk ke root terlebih dahulu
└─# apt install sudo

#Edit dan tambahkan username pada file group
└─# nano /etc/group

sudo:x:27:(username yang ingin diberi akses sudo)
contoh : sudo:x:27:antix

#Mulai ulang Linux
└─# systemctl reboot
